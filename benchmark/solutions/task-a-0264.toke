m=absmin;i=j:std.json;i=s:std.str;f=main():i64{let input=j.parse(s.argv(1));let arr=input;let val=arr.get(0);let other=arr.get(1);if(val=other){<val};let absval=val;if(val<0){absval=0-val};let absother=other;if(other<0){absother=0-other};if(absval<absother){<val};<other};
