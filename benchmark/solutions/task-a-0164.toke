m=bitpack;i=j:std.json;i=s:std.str;f=main():i64{let input=j.parse(s.argv(1));let bits=input;let result=mut.0;let n=bits.len;lp(let i=0;i<n;i=i+1){let bit=bits.get(i);if(bit=1){result=result+1};};j.print(result);};
