m=rotation;i=j:std.json;i=s:std.str;f=main():i64{let input=j.parse(s.argv(1));let a=input.get(0);let b=input.get(1);let result=rotateCheck(a;b);j.print(result);};f=rotateCheck(a:Str;b:Str):bool{if(a.len=b.len){let doubled=a+a;let found=mut.false;lp(let i=0;i<doubled.len-b.len+1;i=i+1){let match=mut.true;lp(let j=0;j<b.len;j=j+1){if(!(doubled.get(i+j)=b.get(j))){match=false;};};if(match){found=true;};};<found}el{<false}};
