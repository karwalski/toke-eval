m=range;i=j:std.json;i=s:std.str;f=main():i64{let input=j.parse(s.argv(1));let arr=input;let n=arr.len;let min=mut.arr.get(0);let max=mut.arr.get(0);lp(let i=1;i<n;i=i+1){let val=arr.get(i);if(val<min){min=val};if(val>max){max=val}};let range=max-min;j.print(range)};
