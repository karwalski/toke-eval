m=ascToString;i=j:std.json;i=s:std.str;f=main():i64{let input=j.parse(s.argv(1));let result=ascToString(input);j.print(result);};f=ascToString(arr:@i64):Str{let n=arr.len;let result=mut."";lp(let i=0;i<n;i=i+1){let code=arr.get(i);let ch=code as u8;result=result+ch as Str};<result};
