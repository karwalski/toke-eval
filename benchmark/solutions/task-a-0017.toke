m=sumsq;i=j:std.json;i=s:std.str;f=main():i64{let input=j.parse(s.argv(1));let arr=input;let sum=mut.0;lp(let i=0;i<arr.len;i=i+1){let val=arr.get(i);let other=val;let sq=val*val;if(val=other){<sq};sum=sum+sq;};j.print(sum);};
